# 原色有感设计系统

## 品牌基调

灵性、自然、明亮、安静。界面以艺术作品和留白作为主要视觉材料，避免后台式卡片堆叠与过度装饰。

## 颜色

- 深海军蓝 `#1B3A6B`：品牌背景与结构色
- 深墨蓝 `#102C52`：沉浸区与页脚
- 青绿 `#329E99`：主要按钮、链接与焦点
- 玫红 `#C8529A`：少量标记与分隔线
- 奶油白 `#F9F4EF`、纸张白 `#FFFAF3`：公共页面背景

正文和交互元素必须满足 WCAG 2.2 AA。键盘焦点统一使用 2px 青绿色描边。

## 字体与版式

- 中文展示：Noto Serif SC，后备 Songti SC
- 中文正文：Noto Sans SC，后备 PingFang SC
- 英文展示：Cormorant Garamond
- 英文正文：DM Sans
- 正文最小 16px；长文最大行宽约 680px；主要区块使用流体间距和编辑式不对称布局

## 图片与动效

- b1 用于首屏和隐私区的铅笔特写；b2–b8 用于品牌故事、流程和作品画廊
- 图片保持真实，不虚构作品作者、用户评价或治疗效果
- 首屏仅通过 `transform` 制造慢速推近与漂移；跑马灯同样只改变 `transform`
- `prefers-reduced-motion` 下关闭全部循环动画，并保持完整静态内容

## 组件规则

- 主按钮使用青绿色圆形胶囊；同一视口只保留一个明显主行动
- 主页锚点为 `philosophy`、`therapy`、`journey`、`gallery`
- 公共路由使用 `/` 与 `/login`；成员路由使用 `/upload`、`/reports`、`/reports/:workId`、`/profile`
- 移动端画廊使用横向吸附浏览，其余内容保持单列自然阅读顺序
